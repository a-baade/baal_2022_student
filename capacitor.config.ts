import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'baal_2022_student',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
